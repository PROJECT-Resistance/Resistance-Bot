const { GuildMember, GuildMemberManager, GuildMemberRoleManager } = require("discord.js");

module.exports = {
    name: 'army',
    description: "accepts application for army server",
    execute(message, args){
        //command goes here
    }
}
